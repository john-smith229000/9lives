extends Node
## DEPRECATED. Scene-scripted sequences now live in SceneFlow subclasses (see
## scene_flow.gd + scene1_flow.gd). This file is intentionally empty and is no
## longer attached to any scene; kept only so its .uid does not dangle.
